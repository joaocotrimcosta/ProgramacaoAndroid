package com.jotap.amelhorcalculadoradaminharua.ui.theme

/*
@Composable
fun HandleButtonClick(buttonText: String ) {

    val updatedValue = if (buttonText == "=") {

        try {
            //val result = calculatorEvaluator.evaluate(variableValue)
            //result.toString()
        } catch (e: Exception) {
            // Log or handle the exception
            "Error"
        }
    } else {

        "$variableValue$buttonText"
    }

    // Notify the updated value to the caller
    onValueChange(updatedValue)
}
*/